spring:
  application:
    name: realmentor
  messages:
    basename: i18n/messages
  datasource:
    url: jdbc:postgresql://localhost:5432/realmentor
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  liquibase:
    enabled: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate # none / validate / create
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: zavarykin87
    password: pisstybaesxozkza
    test-connection: true
    from: zavarykin87@gmail.com
    properties:
      "[mail.smtp.auth]": true
      "[mail.smtp.starttls.enable]": true
      "[mail.debug]": false
